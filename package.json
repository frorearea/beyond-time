{
  "name": "witch-shelter",
  "version": "0.1.0",
  "private": true,
  "description": "A small local witch-chat sanctuary with a configurable LLM backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "flutter build web && node server.js",
    "build": "node scripts/build-sites.mjs",
    "build:web": "flutter build web",
    "build:windows-exe": "node scripts/build-windows-bundle.js",
    "release": "flutter build web && node scripts/build-windows-bundle.js"
  },
  "dependencies": {
    "@yao-pkg/pkg": "^6.22.0",
    "resedit": "^3.0.0"
  }
}
