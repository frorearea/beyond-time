name: beyond_time
description: "A quiet witch-library character agent sanctuary."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  # SDK 自带，不引入任何第三方依赖（flutter_test 随 Flutter SDK 分发）。
  # 用于 test/ 下的会话与页面冒烟测试。
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/ereta-cropped-display.webp
    - assets/prompts/ereta_persona.txt
    - assets/sounds/

  fonts:
    - family: CormorantGaramond
      fonts:
        - asset: assets/fonts/CormorantGaramond-Italic.ttf
    - family: LXGWWenKai
      fonts:
        - asset: assets/fonts/LXGWWenKai-Regular.ttf
    - family: NotoSerifSC
      fonts:
        - asset: assets/fonts/NotoSerifSC-subset.ttf
    - family: SimHei
      fonts:
        - asset: assets/fonts/SimHei.ttf
